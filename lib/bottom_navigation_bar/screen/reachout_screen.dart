import 'package:flutter/material.dart';

class ReachOutScreen extends StatelessWidget {
  const ReachOutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ReachOutScreen Pending")),
    );
  }
}
