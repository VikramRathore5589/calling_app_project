import 'package:flutter/material.dart';

class DialPadScreen extends StatelessWidget {
  const DialPadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("DialPadScreen Pending")),
    );
  }
}
